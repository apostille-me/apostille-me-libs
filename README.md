# apostille-me-libs

Reusable apostille domain, document-status, provider, and API contract libraries.

Initialized through `DEN-1951` as a testable `libs` foundation. Product behavior continues through focused pull requests.

```bash
python3 scripts/verify_repo.py
```
